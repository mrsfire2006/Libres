import { LoginForm } from "@/features/auth/components/login-form";

export default function LoginPage() {
    return <main className="bg-background w-full h-dvh flex justify-center items-center">
        <LoginForm />
    </main>;
}